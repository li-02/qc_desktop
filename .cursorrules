# QC Desktop 项目开发规则

## 语言规范

### 中文优先原则
**所有面向用户的文本内容必须使用中文**，包括但不限于：

- 用户界面文本（按钮、标签、提示信息等）
- 数据版本名称（如"原始导入"、"异常值处理"等）
- 错误提示和警告信息
- 帮助文档和说明文字
- 日志中面向用户的部分
- 数据库中存储的用户可见字段（如备注、描述等）

### 例外情况
以下情况可以使用英文：
- 代码变量名、函数名、类名
- 技术术语（如 API、JSON、CSV 等）
- 第三方库的配置项
- 开发者注释（可选中英文）
- Git 提交信息（可选中英文）

### 实施要求
1. 新增功能时，所有用户可见文本必须使用中文
2. 修改现有功能时，如发现英文文本应同步改为中文
3. 数据库字段默认值、枚举值等涉及用户展示的内容使用中文
4. 前端组件的 label、placeholder、title 等属性使用中文
5. 后端返回给前端的消息、状态描述等使用中文

## 代码风格

### CSS 样式规范
- **优先使用原生 CSS**，而非 TailwindCSS
- 遵循项目现有的设计系统（Glassmorphism、Emerald 主题色）
- 使用 CSS 变量保持一致性

### 设计系统
- 主色调：Emerald Green (`#10b981`)
- 视觉风格：毛玻璃效果（Glassmorphism）、柔和渐变、浮动卡片
- 交互：平滑过渡、悬停提升效果、缩放动画

## 开发流程

### 渐进式开发
- 通过多轮对话迭代明确需求
- 开始设计或编码前完成前期调研
- 遵循"构思方案 → 提请审核 → 分解任务"的流程

### 核心原则
- **简洁至上**：遵循 KISS 原则，避免过度工程化
- **深度分析**：基于第一性原理思考问题
- **事实为本**：以事实为准则，发现错误及时纠正

## 项目特定规则

### 生态数据分析
- 关注传感器数据特性（如太阳能供电导致的昼夜缺失模式）
- 提供专业的数据质量分析工具
- 支持时间模式分析（小时分布、月度分布、热力图等）

### 数据处理
- 支持用户自定义缺失值类型并全局持久化
- 异常值检测时记录每列的缺失值数量
- 列选择时严格按用户选择执行，不默认扩展到所有列
